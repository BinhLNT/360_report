# Thư viện bên thứ ba cần cài (pip install -r requirements.txt)
pandas
jinja2
matplotlib
openpyxl          # đọc/ghi Excel (.xlsx) cho chế độ batch (File thứ 4)
playwright        # xuất PDF hàng loạt bằng headless Chromium (Phase 3)
flask             # giao diện web (dashboard) — webapp.py

# Các module sau là STANDARD LIBRARY của Python (không cần cài):
#   base64, io, os, datetime, csv, json, argparse, unicodedata, http.server, urllib
#
# SAU KHI cài playwright, PHẢI tải trình duyệt Chromium 1 lần:
#   python -m playwright install chromium
#
# LƯU Ý cho WeasyPrint (chỉ dùng ở main.py 1-báo-cáo) trên WINDOWS:
#   WeasyPrint cần GTK3/Pango native. Chế độ BATCH (batch_report.py) dùng
#   Playwright nên KHÔNG cần GTK.
#   GTK3 Runtime: https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer/releases
